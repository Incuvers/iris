#!/bin/bash
export FLASK_ENV=development
export SNAP_COMMON="/etc/iris/SNAP_COMMON"
export MONTIOR_NETPLAN="/etc/netplan"

uuid=$(cat "$SNAP_COMMON"/certs/uuid.txt)
export UUID=$uuid

python3 -m flask run -h '0.0.0.0' --port 80 --no-reload
